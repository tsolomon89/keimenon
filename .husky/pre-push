#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run repository doctors, SQLite checks, and hygiene validations before push
echo "Running repository pre-push checks..."
npm run doctor:runtime && npm run sqlite:check && npm run ci:hygiene:check

